#include <stdio.h>
#include <math.h>
 
int main() {
    double x1, y1, x2, y2;
    scanf("%lf %lf %lf %lf", &x1, &y1, &x2, &y2);
    x1 = (x2 -x1) * (x2 - x1);
    y1 = (y2 -y1) * (y2 - y1);
    x1 = x1 + y1;
    
    x1 = sqrt(x1);
    printf("%.4lf\n", x1);
 
    return 0;
}
