#include <stdio.h>
 
int main() {
 
    int distancia;
    double litros, consumo;
 
    scanf("%d %lf", &distancia, &litros);
    
    consumo = distancia / litros;
 
    printf("%.3lf km/l\n", consumo);
 
    return 0;
}
