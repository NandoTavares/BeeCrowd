#include <stdio.h>
 
int main() {
 int A[20];
 int p=0;
 for(int i = 0; i < 20; i++){
     scanf("%d", &A[i]);
 }
 for(int j = 19; j >= 0; j--){
     printf("N[%d] = %d\n", p, A[j]);
     p++;
 }
    return 0;
}
