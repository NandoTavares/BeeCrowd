#include <stdio.h>
 
int main() {
 
    float a1, b1, a2, b2, c1, c2;
    scanf("%f %f %f %f %f %f", &c1, &a1, &b1, &c2, &a2, &b2);
    c1 = a1*b1;
    c2 = a2*b2;
    c1 = c1+c2;
    printf("VALOR A PAGAR: R$ %.2f\n", (float)c1);
 
    return 0;
}
