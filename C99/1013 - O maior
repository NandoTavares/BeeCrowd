#include <stdio.h>
 
int main() {
    int A = 0, maior = 0;
    for(int i = 0; i < 3; i++){
        scanf("%d", &A);
        if(A > maior){
            maior = A;
        }
        
    }
    printf("%d eh o maior\n", maior);
 
    return 0;
}
